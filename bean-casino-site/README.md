# Bean Casino

This is a Vite React site exported from the ChatGPT/Replit casino code.

## Deploy on Netlify
1. Upload this whole folder to GitHub.
2. In Netlify, import the GitHub repo.
3. Build command: `npm run build`
4. Publish directory: `dist`

The site uses localStorage for usernames and balances, so saves are per browser/device.
